{
  "bomFormat": "CycloneDX",
  "specVersion": "1.6",
  "version": 1,
  "vulnerabilities": [
    {
      "id": "CVE-2026-64607",
      "description": "Apache HttpComponents Client, when using the classic (blocking) I/O model, fails to release the underlying connection back to the connection manager if the response contains an invalid or unsupported Content-Encoding header value. Repeated requests to a malicious or misbehaving server leak connections and eventually exhaust the connection pool, producing a denial of service. The async I/O model is not affected. Fixed in version 5.6.3.",
      "recommendation": "Upgrade to the latest Liquibase Secure release when available. The one reachable usage path is additionally protected by only ever talking to a trusted, TLS-authenticated AWS endpoint.",
      "ratings": [
        {
          "source": {
            "name": "NVD"
          },
          "score": 5.3,
          "severity": "medium",
          "method": "CVSSv31",
          "vector": "CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:L"
        },
        {
          "source": {
            "name": "Liquibase"
          },
          "score": 0,
          "severity": "none",
          "method": "CVSSv31",
          "vector": "CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N",
          "justification": "The dependency is already pinned past the fixed version (httpclient5 5.6.4 >= 5.6.3) across every module that resolves it, and the one reachable classic-client usage path only ever talks to a trusted, TLS-authenticated first-party AWS endpoint, not an attacker-controlled server.\n"
        }
      ],
      "cwes": [
        772
      ],
      "advisories": [
        {
          "url": "https://github.com/advisories/GHSA-hjcp-jmpx-g3qm",
          "title": "GitHub Advisory"
        },
        {
          "url": "https://nvd.nist.gov/vuln/detail/CVE-2026-64607",
          "title": "NVD Entry"
        }
      ],
      "analysis": {
        "state": "not_affected",
        "detail": "httpclient5 is already pinned to 5.6.4 (past the 5.6.3 fix), but that alone only holds until a future downgrade, so we also checked reachability of the classic (blocking) client this CVE affects. Two usages exist: a bundled PII-detection filter, which is unreachable - it requires a policy-loading path Liquibase's checks engine never invokes - and the AWS SDK's synchronous Secrets Manager client, which is real but only ever talks to AWS's own TLS-authenticated endpoint. Exploitation needs that server to repeatedly send malformed Content-Encoding responses, so a remote attacker can't trigger this without already controlling or intercepting trusted AWS traffic - the same precondition used for the related Netty HTTP/2 CVEs.",
        "justification": "requires_environment",
        "response": [
          "update"
        ],
        "firstIssued": "2026-09-21T00:00:00Z"
      },
      "affects": [
        {
          "ref": "pkg:maven/org.apache.httpcomponents.client5/httpclient5",
          "versions": [
            {
              "range": "vers:maven/>=5.0.0-alpha1|<5.6.3",
              "status": "unaffected"
            }
          ]
        }
      ]
    }
  ]
}
