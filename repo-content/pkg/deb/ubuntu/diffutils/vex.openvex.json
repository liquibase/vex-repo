{
  "@context": "https://openvex.dev/ns/v0.2.0",
  "@id": "https://openvex.dev/docs/public/vex-5664f49dae15aa1772806fbf2ea2aba29a090e0d14db364e401aa605042ff634",
  "author": "Liquibase Security Team",
  "timestamp": "2026-09-25T20:13:00.074509529Z",
  "version": 1,
  "statements": [
    {
      "vulnerability": {
        "name": "CVE-2026-53910"
      },
      "timestamp": "2026-09-25T20:13:00.074510811Z",
      "products": [
        {
          "@id": "pkg:deb/ubuntu/diffutils"
        }
      ],
      "status": "not_affected",
      "status_notes": "diff3 tool from GNU diffutils is vulnerable to a heap-based buffer overflow due to multiple signed integer overflows in line-mapping calculations. Incorrect arithmetic in mapping line ranges can result in corrupted values being used for memory allocation and loop bounds. When processing crafted diff output, these overflows may cause the application to allocate insufficient memory and subsequently perform out-of-bounds writes during internal processing. An attacker who can control the output of the diff program used by diff3 (for example, via --diff-program pointing to a malicious script) can trigger out- of-bounds writes, resulting in a crash and potentially remote code execution depending on the environment.",
      "justification": "vulnerable_code_not_in_execute_path",
      "impact_statement": "This issue requires invoking GNU diffutils diff3 on attacker- controlled output from a custom diff program via --diff-program. Liquibase Secure inherits diffutils from the Ubuntu base image, but the stock container executes the Liquibase Java CLI and a focused source review found no references to diff3 or --diff-program anywhere in the runtime path. The vulnerable diff3-specific code path is not reachable from the stock runtime."
    }
  ]
}
