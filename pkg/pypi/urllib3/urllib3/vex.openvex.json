{
  "@context": "https://openvex.dev/ns/v0.2.0",
  "@id": "merged-vex-d390200b46754fcaf9ff82d3488aef9bb260a5b343184365aa24d766d91e87a0",
  "author": "Liquibase Security Team",
  "timestamp": "2026-08-07T16:25:36.188862561Z",
  "version": 1,
  "statements": [
    {
      "vulnerability": {
        "name": "CVE-2023-43804"
      },
      "timestamp": "2026-08-07T16:25:34.754953336Z",
      "products": [
        {
          "@id": "pkg:pypi/urllib3"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_in_execute_path",
      "impact_statement": "urllib3 cookie header leak vulnerability. urllib3 is bundled as a transitive dependency within GraalVM Python extensions but its HTTP client functionality is never invoked at runtime. No HTTP requests are made via Python at runtime."
    },
    {
      "vulnerability": {
        "name": "CVE-2025-66418"
      },
      "timestamp": "2026-08-07T16:25:34.479399255Z",
      "products": [
        {
          "@id": "pkg:pypi/urllib3"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_in_execute_path",
      "impact_statement": "urllib3 vulnerability. urllib3 is bundled as a transitive dependency within GraalVM Python extensions but its HTTP client functionality is never invoked at runtime. All Python packages are pre-installed at build time via Maven graalpy-maven-plugin; no HTTP requests are made via Python at runtime."
    },
    {
      "vulnerability": {
        "name": "CVE-2025-66471"
      },
      "timestamp": "2026-08-07T16:25:34.569569244Z",
      "products": [
        {
          "@id": "pkg:pypi/urllib3"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_in_execute_path",
      "impact_statement": "urllib3 vulnerability. urllib3 is bundled as a transitive dependency within GraalVM Python extensions but its HTTP client functionality is never invoked at runtime. All Python packages are pre-installed at build time via Maven graalpy-maven-plugin; no HTTP requests are made via Python at runtime."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-21441"
      },
      "timestamp": "2026-08-07T16:25:34.664019106Z",
      "products": [
        {
          "@id": "pkg:pypi/urllib3"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_in_execute_path",
      "impact_statement": "urllib3 vulnerability. urllib3 is bundled as a transitive dependency within GraalVM Python extensions but its HTTP client functionality is never invoked at runtime. All Python packages are pre-installed at build time via Maven graalpy-maven-plugin; no HTTP requests are made via Python at runtime."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-44431"
      },
      "timestamp": "2026-08-07T16:25:35.5449592Z",
      "products": [
        {
          "@id": "pkg:pypi/urllib3"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_in_execute_path",
      "impact_statement": "urllib3 vulnerability. urllib3 is bundled as a transitive dependency within GraalVM Python extensions but its HTTP client functionality is never invoked at runtime. All Python packages are pre-installed at build time via Maven graalpy-maven-plugin; no HTTP requests are made via Python at runtime."
    },
    {
      "vulnerability": {
        "name": "CVE-2026-44432"
      },
      "timestamp": "2026-08-07T16:25:35.638223413Z",
      "products": [
        {
          "@id": "pkg:pypi/urllib3"
        }
      ],
      "status": "not_affected",
      "justification": "vulnerable_code_not_in_execute_path",
      "impact_statement": "urllib3 vulnerability. urllib3 is bundled as a transitive dependency within GraalVM Python extensions but its HTTP client functionality is never invoked at runtime. All Python packages are pre-installed at build time via Maven graalpy-maven-plugin; no HTTP requests are made via Python at runtime."
    }
  ]
}
